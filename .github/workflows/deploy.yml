name: Deploy to Render

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          echo "Pushed to main - Render will auto-deploy via webhook"
          echo "No manual deployment needed - Render watches the repo"
